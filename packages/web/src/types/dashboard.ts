import { 
  Job, 
  Application, 
  JobStats, 
  SavedView, 
  KanbanStatus, 
  DashboardView, 
  BoardMode,
  SalaryInfo
} from "@hireall/shared";

export type ApplicationStatus = 
  | "saved"
  | "applied"
  | "offered"
  | "rejected"
  | "withdrawn";

export type { 
  Job, 
  Application, 
  JobStats, 
  SavedView, 
  KanbanStatus, 
  DashboardView, 
  BoardMode,
  SalaryInfo
};